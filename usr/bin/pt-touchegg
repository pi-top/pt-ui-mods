#!/bin/bash
###############################################################
#                Unofficial 'Bash strict mode'                #
# http://redsymbol.net/articles/unofficial-bash-strict-mode/  #
###############################################################
set -euo pipefail
IFS=$'\n\t'
###############################################################

if [ "$EUID" -eq 0 ]; then
	echo "This command does not support being run as the root user."
	exit
fi

pt-notify-send -i libinput-gestures -t 10000 "Touchégg - Gestures Manager" "Multi-touch gestures are now enabled - happy swiping!"
touchegg
